/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['atlas-content-cdn.pixelsquid.com','images.unsplash.com', 'res.cloudinary.com'],
  },
}

export default nextConfig
